kubectl get secrets -o jsonpath="{.items[?(@.metadata.annotations['kubernetes\.io/service-account\.name']=='argocd-admin')].data.token}" | base64 -di > output.txt
add namespace to init manifest
get the auto generated token from vcluster, add it to cluster secret, and create cluster secret

kubectl config view --raw -o go-template='{{index ((index (index .clusters 0) "cluster")) "certificate-authority-data"}}' > cadata.txt
