kubectl get secrets -o jsonpath="{.items[?(@.metadata.name=='argocd-admin-token-kpncf')].data.token}" -n orders-vcluster | base64 --decode > output.txt
add namespace to init manifest
get the auto generated token from vcluster, add it to cluster secret, and create cluster secret

kubectl config view --minify --raw --output 'jsonpath={..cluster.certificate-authority-data}' > cadata.txt

---
              apiVersion: v1
              kind: Secret
              metadata:
                name: argocd-admin-token
                namespace: {{cluster.namespace}}
                annotations:
                  kubernetes.io/service-account.name: argocd-admin
              type: kubernetes.io/service-account-token